<div class="p-5 mb-4 text-light rounded-3 text-start">
  <div class="container-fluid py-5">
    <h1 class="display-5 fw-bold"><%= author.first_name %> <%= author.last_name %></h1>
    <p class="card-text">
      <strong>Country:</strong>
      <%= author.country %>
    </p>

    <p class="card-text">
    <table class="table table-hover text-light table-active text-center">
      <thead>
      <tr>
        <th scope="col">Title</th>
        <th scope="col">ISBN</th>
        <th scope="col">Author</th>
        <th scope="col">Action</th>
      </tr>
      </thead>
      <tbody>
      <% @author.books.each do |book| %>
        <tr>
          <td><%= book.title %></td>
          <td><%= book.isbn %></td>
          <td><%= book.author.first_name + ' ' + book.author.last_name %></td>
          <td><%= link_to "Show", book, class:"text-info" %></td>
        </tr>
      <% end %>
      </tbody>
    </table>
    </p>
    <br>
    <div class="d-flex justify-content-center">
      <%= link_to "Edit", edit_author_path(@author), class:"btn btn-outline-light me-3" %>
      <%= link_to "Add Book", new_book_path(:new_book_author_id => "#{@author.id}"), class:"btn btn-outline-success me-3" %>
      <%= link_to "Back", authors_path, class:"btn btn-outline-primary me-3" %>
      <%= button_to "Delete", @author, method: :delete, form: { data: { turbo_confirm: "Are you sure?" } }, class:"btn btn-outline-danger me-3" %>
    </div>
  </div>
</div>